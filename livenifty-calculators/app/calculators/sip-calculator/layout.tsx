export const metadata = {
  title: "SIP Calculator – Calculate Mutual Fund SIP Returns",
  description:
    "Free SIP calculator to estimate mutual fund returns. Plan your monthly investments and future wealth easily.",
};

export default function Layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
